# Progress Log

One line a day. Honesty > streaks. This is experiment data.

Format: `Day NN (YYYY-MM-DD) — ✅/⏭️ — what clicked / what confused me / mins spent`

---

- Day 01 (2026-07-07) — ✅ — temp-converter CLI. Compiler taught `:=` vs `=`, no implicit int↔float64 conversion, and zero values. Smooth, ~20 min. Filed for later: `==` on floats, gofmt tabs.
- Day 02 (2026-07-08) — ✅ — `iota` enum for job Status + `String()` method. Enum LOGIC was correct first try; all friction was prose-bleeding-into-code + a missing brace. Takeaway: adopt a Go editor / run `gofmt` live, and fix the FIRST compiler error first.
